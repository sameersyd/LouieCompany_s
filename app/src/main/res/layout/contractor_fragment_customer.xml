<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.SwipeRefreshLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/contFrag_referral_swipeRefreshLayout"
    android:layout_height="match_parent"
    android:background="@drawable/view_gradient">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ListView
            android:id="@+id/contFrag_referral_listview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@null"/>

        <ImageView
            android:id="@+id/contractor_customer_addImage"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:layout_marginBottom="70dp"
            android:src="@drawable/add_float"
            android:layout_gravity="center_horizontal|bottom"/>

    </FrameLayout>

</android.support.v4.widget.SwipeRefreshLayout>
